# Oracle OpenTelemetry Showcase — Observability Report

**Service:** `{{ service_name }}`
**Generated:** {{ generated_at }}
**Lookback window:** {{ lookback_minutes }} minutes

---

## Summary

| Metric | Value |
|--------|-------|
| Total Operations | {{ "{:,.0f}".format(metrics.total_operations) }} |
| Total Errors | {{ "{:,.0f}".format(metrics.total_errors) }} |
| Error Rate | {{ "%.2f"|format(metrics.error_rate_pct) }}% |
| Query Latency p50 | {{ "%.1f"|format(metrics.query_p50_ms) }} ms |
| Query Latency p95 | {{ "%.1f"|format(metrics.query_p95_ms) }} ms |
| Query Latency p99 | {{ "%.1f"|format(metrics.query_p99_ms) }} ms |
| Peak Pool Busy | {{ "%.0f"|format(metrics.pool_max_busy) }} connections |
| Avg Vector Similarity (p50) | {{ "%.3f"|format(metrics.avg_vector_similarity) }} |

---

## Operations Breakdown

| Operation | Count |
|-----------|-------|
{% for op, count in ops_by_type.items() | sort(attribute='1', reverse=True) -%}
| {{ op }} | {{ "{:,}".format(count) }} |
{% endfor %}

---

## Slowest Traces (>50 ms)

{% if slow_traces %}
| Trace ID | Root Span | Duration | Spans |
|----------|-----------|----------|-------|
{% for trace in slow_traces -%}
| `{{ trace.traceID[:16] }}…` | {{ trace.rootSpanName | default("—") }} | {{ format_duration_ms(trace.durationMs) }} | {{ trace.spanCount | default("—") }} |
{% endfor %}
{% else %}
_No slow traces found in the selected window._
{% endif %}

---

_Report generated by [oracle-otel-showcase](https://github.com/your-username/oracle-otel-showcase)_
